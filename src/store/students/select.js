export const selectStudent = (state) => state.students.studentsList;
export const selectPerPage = (state) => state.students.perPage;
export const selectTotalPages = (state) => state.students.totalPages;
export const selectPage = (state) => state.students.page;
export const selectCheckedStudents = (state) => state.students.selectedStudents;